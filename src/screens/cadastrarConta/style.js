import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    label: {
        marginLeft: 10
    },
    input: {
        borderBottomWidth: 1,
        borderColor: '#AAA',
        marginBottom: 20,
        marginTop: -5,
        paddingBottom:-5,
        color: '#000'
    },
})

export default style;
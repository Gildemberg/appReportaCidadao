import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    scrollContent: {
    },
    titulo:{
        fontSize: 40,
        marginBottom: 40,
        alignSelf: 'center'
    },
    texto: {
        color: '#fff',
        fontSize: 20,
        padding: 10
    }
})
export default style;